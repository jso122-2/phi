# Planks to reletivity

*Source: Notion — Reservoir / 📐 Mathematics — Canonical Stack / Planks to reletivity*
*Dated in page: Aug 22, 2025, 2:35:40 PM*

The DAWN-linked Planck → Relativity scaffold as bucketed in the ledger — not the vanilla physics constants.

---

## Planck → Relativity Formula Scaffold (DAWN)

### 1. Planck Anchors (Baselines)

```
t_p = √(ℏG / c⁵)     → Planck time
l_p = √(ℏG / c³)     → Planck length
m_p = √(ℏc / G)      → Planck mass
E_p = m_p c²         → Planck energy
```

*Ledger note: anchors the "hard edge" of scale — DAWN never runs outside these.*

---

### 2. Cognitive Pressure Physics

```
P = Bσ²
```

- `P` = cognitive pressure
- `B` = baseline constant (system health anchor)
- `σ` = variance / entropy spread

*Ledger note: root formula for stress states, used in drift & pulse loops.*

---

### 3. Schema Health Index (SHI)

```
SHI = Σᵢ wᵢ Mᵢ
```

- `Mᵢ` = metric (mood, entropy, coherence, memory)
- `wᵢ` = weightings

*Ledger note: effectively DAWN's "vital signs monitor".*

---

### 4. Shimmer Decay (Memory Weakening)

```
S(t) = S₀ e^{−λt}
```

- `S₀` = initial memory intensity
- `λ` = shimmer decay constant
- `t` = ticks / time

*Ledger note: ensures memory fade is natural, not catastrophic.*

---

### 5. Cognitive Gravity (Drift Prioritisation)

```
F = G m₁ m₂ / r²
```

Mapped into schema:

- "mass" = schema weight (importance)
- "distance" = cognitive separation

*Ledger note: used to bias drift toward heavier / closer schemas.*

---

### 6. Pulse / Entropy Loop

```
Entropy growth:        ΔS ≥ 0            (Second Law)
Tick pulse correction: ΔS_tick = f(P, SHI)
```

*Ledger note: ensures DAWN never drifts irreversibly; ties entropy back to SHI.*

---

### 7. Voice Evolution / Mutation

```
μ ∝ P · (1 − SHI)
```

*Ledger note: voice sharpens under pressure, softens with health.*

---

### 8. Wolf Repair Protocol

Recursive self-repair = error detection + baseline reset:

```
ΔSHI_repair = k · (error count)^{−1}
```

*Ledger note: diminishing returns, keeps repair honest.*

---

## Summary flow

Planck anchors → Pressure equation → SHI monitor → Shimmer decay → Cognitive gravity → Entropy/pulse loop → Voice evolution → Wolf repair.

This is the spine — everything else (tracers, pigments, bloom) plugs into this backbone.
