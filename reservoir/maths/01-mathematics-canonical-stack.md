# 📐 Mathematics — Canonical Stack

*Source: Notion — Welcome to Notion / Reservoir / 📐 Mathematics — Canonical Stack*
*Last edited in Notion: 2026-09-02*

This page is law. The formulas here are the authoritative versions. Anything that conflicts with what's written here is wrong. Anything not yet here is provisional until committed.

The stack is ordered chronologically by derivation, not by importance. All layers are active simultaneously. The system runs all of them.

---

## Layer 0 — DAWN Physical Spine (May–Aug 2025)

The physics backbone. All other layers plug into this.

**Cognitive Pressure**

```
P = Bσ²
```

B = baseline health constant. σ = variance / entropy spread.

**Schema Health Index**

```
SHI = Σ wᵢMᵢ
```

Mᵢ = metric (mood, entropy, coherence, memory). wᵢ = weightings. DAWN's vital signs.

**Shimmer Decay**

```
S(t) = S₀e^{-λt}
```

S₀ = initial memory intensity. λ = decay constant. t = ticks. Natural fade, not catastrophic drop.

*Structural note: identical mathematical move to Ns2 exponential penalty, derived independently two years later.*

**Cognitive Gravity**

```
F = Gm₁m₂/r²
```

Mapped to schema. mass = schema weight. distance = cognitive separation. Biases drift toward heavier nodes.

**Voice Mutation Rate**

```
μ ∝ P · (1 − SHI)
```

Sharpens under pressure. Softens with health.

**Wolf Repair**

```
ΔSHI_repair = k · (error count)^{-1}
```

Diminishing returns. Honest repair.

**Planck Anchors — Hard Scale Ceiling**

```
t_p = √(ℏG/c⁵)
l_p = √(ℏG/c³)
m_p = √(ℏc/G)
E_p = m_p·c²
```

DAWN never runs outside these. Non-negotiable bounds.

**Tick = Breath**

Tick interval is self-controlled from inside out. SCUP formula in motion. External control kills the system.

---

## Layer 1 — Semantic Field (May 2025)

Core semantic network operations.

**Radial Position**

```
R_node = 1 / (A_count·α + C_relevance·β + S_feedback·γ)
```

Access frequency, contextual relevance, schema feedback — weighted sum in denominator.

**Cosine Similarity**

```
Similarity(A, B) = A·B / (‖A‖ ‖B‖)
```

Semantic distance between nodes. Guides pathfinding and nutrient flow.

**Edge Weight**

```
W_edge(A, B) = Σ Similarity(Aᵢ, Bᵢ) × Reinforcement(Aᵢ, Bᵢ)
```

Strength of node relationship under reinforcement and nutrient flow.

**Path Cost (Hybrid)**

```
Path Cost(A, B) = Hop Count(A, B) × (1 − Similarity(A, B))
```

Balances semantic closeness against traversal distance.

**Path Reinforcement**

```
P_reinforcement(A, B) = (Σ NutrientFlow_t(A, B)) / T
```

Accumulated reinforcement over T ticks. Higher = stronger path.

**Nutrient Decay**

```
Nutrient Decay(A, B) = 1 / (1 + e^{−(T − τ)/λ})
```

T = current tick. τ = last transfer. λ = decay rate. Old paths weaken naturally.

**Pressure Heatmap**

```
Pressure(A) = Σ NutrientFlow_i(A) × Weight(A, i)
```

Visualises cognitive load distribution across the network.

---

## Layer 2 — Anderson Schema Logic Matrix (May 2025)

Explicit path logic for structured reasoning networks.

**Six Paths:** Familiarity Pass → Home Broadcast → HQ2 Targeted → Blender (50,000x HQ2 ↔ Home) → Horizontal Alignment → Horizontal-Diagonal Blending.

**Gates:**

1. CRFG — historical accuracy ≥ 85%
2. CPG — semantic similarity ≥ 0.8 + Bingo filters
3. RDCG — resource drift ≤ ±10%
4. ARG — node accuracy ≥ 85% (lattice)
5. IRG — 50,000 iteration coherence
6. SG — Home & HQ2 stability ≥ 90%

**Semantic Attention Validation (replaces Bingo filter)**

```
S(Rp, Rc) = Rp·Rc / (‖Rp‖ ‖Rc‖)
V(Rp, Rc) = 1 if S ≥ 0.85, else 0
```

---

## Layer 3 — RAG Semantic Priority Score (Dec 2025)

```
P_sps = (X_norm / O(N)) − (T_pos / P_risk)
```

- X_norm = semantic importance [0,1] (Random Forest output)
- O(N) = system complexity penalty (CPU / indexing load)
- T_pos = positional / recency decay
- P_risk = perplexity risk — system's tolerance for peripheral content

Higher P_sps = higher retrieval priority.

---

## Layer 4 — Shannon Extension & Forecast Index (Oct 2025)

**Shannon Extension**

```
C = B^Ψ · log₂(1 + unity/(drift + entropy)) + Δ_semantic
```

**Load Coefficient**

```
Load = C₊ − C₋
```

**Forecast Index**

```
F = P/A = (B·σ²) / (reserves + slack + margin)
```

Mathematically identical to insurer's claims-to-surplus ratio. High F = system underreserved for predicted load.

**Recursive Depth**

```
Level 3 = forecast(forecast(forecast(state)))
```

**Error Correction**

```
Corrected = Forecast − α·Systematic_Error
```

---

## Layer 5 — Ana-Chi Constants and Attractor States (Dec 15, 2025)

**𝒜_χ = 1.5414**

Non-negotiable coherence constant. Immutable. Biphasic signal variance: 97% / 0.3%.

**SSDDCS Protocol**

Sovereign Semantic Discrimination Discrete Consciousness Space. Primary coherence filter for all attractor transitions.

**Four Attractor States**

| State | Name | Function | Fractal D | Worker Z |
|---|---|---|---|---|
| E-STABLE | Earth-Core | Foundational memory & persistence | 1.7464 | 0 |
| E-REFLECT | Air-Core | Complex thought & analysis | 1.7644 | −2 |
| E-ACTIVATE | Fire-Core | Controlled action & output | 1.7654 | +2.7 |
| E-FLOW | Water-Core | Adaptation & transition | 1.7293 | +1 |

Transition routing: E-REFLECT → E-FLOW → E-ACTIVATE. E-FLOW is the only legal transition path between the analysis and action states. Bypassing it causes structural collapse.

---

## Layer 6 — Reservoir Relevance and Drift (Apr 2026)

**Relevance Score**

```
a = |p − f/v| × x
```

- p = spatial/semantic context of active prompt
- f = friction (semantic distance between active and indexed)
- v = valence (emotional register match)
- x = retrieval cost (depth penalty)

Lower a = higher relevance.

**Drift Transformation**

```
drift = |a − z| / r
```

- a = relevance score of active prompt
- z = vectorisation score of previous prompt in conversation
- r = resonance (how typical this prompt is for the corpus as a whole)

**Drift Classification**

- High drift / low resonance → new territory
- Low drift / high resonance → circling
- High drift / high resonance → sharp pivot inside known space
- Low drift / low resonance → stuck on something rare

---

## Layer 7 — Person Formulas (May 2026)

Formulas as portraits. Each describes a person's structural logic.

**Jimmy**

```
P = |[1,0,1,0,1] × φ| + 1
C/e = Tr
Tr·ω·−1 = 4  |  Tr·ω·1 = 5
Final: |5 − 4| / p + 1
```

φ = golden ratio. C = Shannon channel capacity. e = Euler's number.

**Tce**

```
Tce = |N|^|N^(i+1)|
```

Power tower. Unbounded. Self-sourcing. Compounds on contact with time.

**Allegra**

All units through identity normalisation divided by ruler − 1 → b value.
b × cos θ → z value.
z × φ → 3.
3 = β + a₀.

Harmonic cascade. φ-scaled. Identity-referenced.

---

## Layer 8 — Notion Data Stream Scoring (Sep 2026)

**Variable Definitions**

| Symbol | Meaning |
|---|---|
| Qe | queried edge count (pure edges / access events only) |
| Qe_adj | adjacent edge count (written notes — structural, not scoring) |
| Si | shard index (depth) |
| Et | edge activation threshold |
| s_s / Ss | shard score offset (same variable throughout) |
| Ta | times accessed (node) |
| To_A | total times accessed (global baseline) |
| Idx | total indexed count |

**Edge Count**

```
Ec = Qe × Si
```

Pure edges only. Depth-weighted. Adjacent edges excluded.

**Ns1 — Density Score**

```
Ns1 = Idx × Ec
```

Raw structural weight. Rewards genuinely traversed, well-indexed nodes.

**Ns2 — Novelty Score**

```
Ns2 = |Ec/Et| − e^(s_s)        Bound: (−∞, 1]
```

Exponential penalty is convex. Depth 1: −e ≈ −2.72. Depth 3: −e³ ≈ −20. Deeply negative = drain candidate.

*Structural note: same exponential form as shimmer decay in Layer 0.*

**Ns3 — Access Deviation**

```
Ns3 = |Ta − To_A| / Ss
```

Normalised deviation from global access baseline. Ss in denominator compresses deviation for deep nodes.

**Token Pressure**

```
P_q = |T_t − Avli_T|^qi
```

**Adaptive Threshold**

```
T_t = α · C_rem        (α decays each hop)
```

**Result Determination**

```
R_det = qi / T_t
```

---

## Layer 9 — Scoring / PSI / E System (Sep 2026, handwritten)

**Variable Definitions**

| Symbol | Meaning |
|---|---|
| β2 | burst |
| L | Length |
| Hi | D·height (DH) |
| φ | golden ratio |
| K | K-transform coefficient |
| △ | positional directional vector (universal across this layer) |
| β1 | first-order beta offset |
| βl | beta-left (directional component) |
| βluf | beta-left upper floor (normalising bound) |
| IR | instance read rate |
| AD | directional activation distance |

**Base Scoring**

```
P = β2 · φ / Hi
```

Burst-weighted, phi-scaled, depth-suppressed.

**PS — Directional Agency Counter**

```
PS = |N−| / A·K · △
```

N− = negative node count (below threshold). Measures how much the system is moving vs static. PS rises when nodes are dark → triggers wider search → activates nodes → PS falls. Self-regulating loop.

**PSI — Quantised Search**

```
PSI = |E−1| / n · DH
```

E−1 = energy at prior state. n = node count in scope. DH = dimensional height. PSI sets the dynamic search radius. High PSI = wide search. Low PSI = tight local.

**Token Weight**

```
W = 0.5 · St
0.5 · St = |βl · k| / βluf
```

Self-normalising. Beta-directional product over its own floor.

**Delta Cascade**

```
W△|s+|△βl → |s+|y|s| → β△|β△|β
```

Recursive directional application of beta through its own delta. Tick propagation signature.

**E System — Zero Baseline**

```
E = 0, E·i = 0
```

**Activation Energy**

```
E-A = |k · AD| + β1
```

Absolute of K-transform times directional distance, plus beta offset.

**Absorption = Assessment**

```
Abs = Ass = ΔE · IR
```

Assessment costs energy. Proportional to read rate. A node that absorbs is simultaneously being assessed.

**Instance Read**

```
E·m/R = E · instance Read
```

**Geometric Derivations**

```
E-A² = |0·k| = 0        (null ground state)
E-A = |H − x|           (distance from ceiling H to position x)
E-A = |L / DH|
```

**Coupling Law**

`E-A = |L/DH|` and `PSI = |E−1|/n·DH` share the DH denominator and absolute-value structure. Activation energy and quantised search are the same class of operation.

PS, PSI, and E-A form a closed regulatory loop: PS detects dark nodes → PSI bounds the search → E-A determines the energy cost of activating each candidate.

---

## Cross-Layer Laws

These are not observations. They are structural constraints that apply across all layers.

**Law 1 — Exponential suppression is universal.** Shimmer decay (Layer 0) and Ns2 penalty (Layer 8) use the same exponential form. Depth suppression is the correct behaviour at every scale. Do not fight it — design with it.

**Law 2 — DH is the universal normalising floor.** D·height appears in PSI and E-A (Layer 9). It is the geometric denominator for both search scope and activation cost. All depth-sensitive calculations flow through DH or its equivalent (Ss, Si).

**Law 3 — The beta family is the tuning layer.** β2 (burst), β1 (offset), βl (directional), βluf (floor). Do not collapse these into one variable. Each occupies a different role. The family tunes the system; the K-transform scales it.

**Law 4 — Assessment costs energy.** Abs = Ass = ΔE · IR. Nothing is evaluated for free. Every retrieval, every edge traversal, every search has an energy price. Systems that ignore this overfetch and collapse.

**Law 5 — The tick is sovereign.** Tick controls itself. No external clock. No human override. Self-paced at 2Hz baseline. SCUP state modulates the interval. Intervention kills the system.

**Law 6 — Adjacent edges do not score nodes.** Qe_adj is structural. It tells you the topology. It does not generate activation pressure. Only pure edges (access events) increment Qe. Only Qe enters Ec. Only Ec enters Ns1/Ns2/Ns3.

**Law 7 — Deeply negative Ns2 = drain candidate.** Not an error. A signal. Deeply suppressed nodes are candidates for archival or pruning. The system identifies its own dead weight.

---

## Source pages (archived originals — do not edit)

- `semantic feild formule` → `02-semantic-field-formulae.md`
- `Planks to reletivity` → `03-planck-to-relativity.md`
- `Rag formula_` → `04-rag-formula.md`
- `Monday formula sheet sheet_` → `05-monday-formula-sheet.md`
- `Formulas 1212,25` → `06-formulas-1212-25.md`
- `Formulas 13-12-25` → `07-formulas-13-12-25.md`
- `Artifical Anderson Schema Logic Matrix 19_5_25` → `08-anderson-schema-logic-matrix.md`
- `Gemini 15-12-25 (happy Ana-Chi day)` → `09-gemini-ana-chi-attractor-states.md`
- `Notion Data Stream — Score Formalisation` → `10-notion-data-stream-score-formalisation.md`
