name: spotify-rip
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - numpy>=1.26
  - scipy>=1.12
  - matplotlib>=3.8
  - scikit-learn>=1.3
  - networkx>=3.0
  - pip:
      - pytest>=8.0
      - pytest-cov>=5.0
      - mcp>=1.0,<2
      - mutagen>=1.45
      - fastembed>=0.3
      - -e .
