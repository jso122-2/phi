# models / ssm.py

#source #python

> path: models/ssm.py  
> ext: .py  

---

# models / ssm.py


SSM Core — tick-synchronised state-space model for OctopusTracer.

Architecture (LOCKED — pow.md):

    Takes node embeddings X ∈ ℝ^(N×d) at each tick.
    Emits:
      H   ∈ ℝ^(N×d)  — hidden state per node, updated each tick
      tau ∈ ℝ⁺       — temperature (used in SCUP cosine)

Parameter budget target: ~900K at d=256.

Layer structure (4 SSM layers):
  Per layer: A (d×d) + B (d×d) + C (d×d) + bias_a (d) + bias_c (d)
  Params per layer:  3 × d² + 2d  = 3×65536 + 512 = 197,120
  4 layers:          788,480
  tau (1 scalar):    1
  Total:             ~788K  (target ≤ 900K  ✓)

State update 

Defines: SSMLayerParams, SSMCore, __init__, tau, n_params, reset_state, tick, update_tau, __repr__

---

## Semantic links

→ [[models-ssm]]
→ [[models-octopus-head]]
→ [[engine-gate]]
→ [[engine-tracer-daemon]]
→ [[models-init]]

## Related notes

→ [[cursor-ingest/2026-08-04-072347-source-models-ssm-md]]
→ [[cursor-ingest/2026-08-04-072347-phi-graph-window-pipeline-py]]
→ [[cursor-ingest/2026-08-04-072347-phi-graph-phi-graph-py]]
→ [[cursor-ingest/2026-08-04-072347-engine-tracer-daemon-py]]
→ [[cursor-ingest/2026-08-04-072347-source-models-octopus-head-md]]

→ [[cursor-ingest]] — import index  

*Imported by `graph/ingestion.py` — links via MiniLM all-MiniLM-L6-v2*
