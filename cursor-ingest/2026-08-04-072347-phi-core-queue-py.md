# phi / core / queue.py

#source #python

> path: phi/core/queue.py  
> ext: .py  

---

# phi / core / queue.py

phi.core.queue — queue modulation engine.

QueueEngine is pure Python: no Tk, no pygame, no I/O.
It owns the ordered list of playlist indices (queue) and the filtered
view of that list (view), plus shuffle / repeat / sort state.

Index spaces
------------
phi uses three separate index spaces that are easy to confuse:

  playlist_idx  — index into Library.playlist (the master track list).
                  Stable: never changes as long as the track is in the library.

  queue_pos     — index into QueueEngine.queue (the playback order list).
                  queue[queue_pos] == playlist_idx.
  

Defines: bind_floor, should_prefetch, QueueEngine, __init__, shuffle, shuffle, current_playlist_idx, rebuild, apply_search, advance, next_user, prev, sort_by_curve, cycle_shuffle, toggle_shuffle, cycle_repeat, sort_by, clear_sort, move_to_next, remove_at_view_indices, nudge, swap_view_rows, _key

---

## Semantic links

→ [[engine-phi-player]]
→ [[engine-prefeed-shuffle]]
→ [[PLAYBACK]]
→ [[mcp-server-tools-prefeed-shuffle]]
→ [[engine-cairrn-dispatch]]

## Related notes

→ [[cursor-ingest/2026-08-04-072347-phi-ui-app-queue-ops-py]]
→ [[cursor-ingest/2026-08-04-072347-phi-core-playback-controller-py]]
→ [[cursor-ingest/2026-08-04-072347-phi-ui-qt-playlist-py]]
→ [[cursor-ingest/2026-08-04-072347-engine-phi-player-py]]
→ [[cursor-ingest/2026-08-04-072347-phi-core-history-py]]

→ [[cursor-ingest]] — import index  

*Imported by `graph/ingestion.py` — links via MiniLM all-MiniLM-L6-v2*
