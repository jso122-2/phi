# phi / models / dragon_curve.py

#source #python

> path: phi/models/dragon_curve.py  
> ext: .py  

---

# phi / models / dragon_curve.py

phi.models.dragon_curve — Dragon curve geometric scoring (pure numpy).

The dragon curve is generated by the recursive fold rule:
    seq(k) = seq(k-1) + [1] + reverse_complement(seq(k-1))
where 1=right-turn, 0=left-turn. This produces a self-similar space-filling
curve whose fold-bit sequence is a natural coarse-to-fine positional encoding.

Geometric variables (from scribble-002, prompt 3):
    D1  — direction vector of the nearest segment to query point (x, y)
    D2  — y-component of the midpoint of the *next* segment  ("vertical slice")
    D3  — absolute 2D endpoint of the next segment
 

Defines: _generate_fold_sequence, _fold_seq_to_points, DragonCurve, __init__, n_segments, points, unit_to_curve, nearest_segment, fold_bits, d1_d2_d3, score_a, score_b, score, score_batch, rolling_d4_a

---

## Semantic links

→ [[workers-cairrn-z-space]]
→ [[models-octopus-head]]
→ [[models-regression]]
→ [[models-init]]
→ [[models-suckers]]

## Related notes

→ [[cursor-ingest/2026-08-04-072347-phi-ui-qt-rooms-dragon-room-py]]
→ [[cursor-ingest/2026-08-04-072347-phi-engine-curve-daemon-py]]
→ [[cursor-ingest/2026-08-04-072347-phi-engine-curve-walker-py]]
→ [[cursor-ingest/2026-08-04-072347-phi-engine-fold-injector-py]]
→ [[cursor-ingest/2026-08-04-072347-phi-models-d4-model-py]]

→ [[cursor-ingest]] — import index  

*Imported by `graph/ingestion.py` — links via MiniLM all-MiniLM-L6-v2*
