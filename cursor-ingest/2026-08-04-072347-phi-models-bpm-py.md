# phi / models / bpm.py

#source #python

> path: phi/models/bpm.py  
> ext: .py  

---

# phi / models / bpm.py

phi.models.bpm — BPM, key, energy, and mood models.

BPMModel priority chain:
  1. Existing tag (TBPM, BPM, tempo) via mutagen — instant, no compute.
  2. librosa.beat.beat_track() on a 90-second audio window — ~1–3 s per track.
  3. Returns {} / pending marker if neither source is available.

KeyModel priority chain:
  1. Existing tag (TKEY / initialkey) — instant.
  2. Krumhansl-Schmuckler chroma correlation via librosa — ~1–2 s per track.

EnergyModel, MoodModel: heuristic from ReplayGain / BPM annotations.


Defines: BPMModel, _pearson, _ks_key, KeyModel, EnergyModel, MoodModel, EmbeddingModel, can_process, run, _from_tags, _from_audio, can_process, run, _from_tags, _from_audio, can_process, run, _read_gain, can_process, run, _classify, can_process, run

---

## Semantic links

→ [[engine-phi-player]]
→ [[engine-phi-session]]
→ [[pipeline-worker-multi-source]]
→ [[scripts-embed-tracks]]
→ [[engine-hot-loader]]

## Related notes

→ [[cursor-ingest/2026-08-04-072347-phi-audio-beat-py]]
→ [[cursor-ingest/2026-08-04-072347-phi-core-smart-playlist-py]]
→ [[cursor-ingest/2026-08-04-072347-phi-ui-qt-rooms-mixer-room-py]]
→ [[cursor-ingest/2026-08-04-072347-phi-core-poll-engine-py]]
→ [[cursor-ingest/2026-08-04-072347-phi-audio-init-py]]

→ [[cursor-ingest]] — import index  

*Imported by `graph/ingestion.py` — links via MiniLM all-MiniLM-L6-v2*
