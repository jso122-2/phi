# workers.md

#doc #md

> path: workers.md  
> ext: .md  

---

# workers

#code

The computation pipeline — Workers wrap callables with logging, timing, and error handling.

---

## Connections

→ [[CODE]] ← code hub  
→ [[HOME]] ← grand central  
→ [[sims]] — SimWorker wraps sims functions  
→ [[mcp-server]] — BatchWorker used inside MCP tools  

---

## `workers/base.py`

### Class hierarchy

```
Worker                ← base; wraps any callable
├── SimWorker         ← wraps a sims.* function (same implementation)
└── BatchWorker       ← runs multiple Workers in sequence
```

---

## `Worker`

```python
w = Worker(name="my-worker", fn=some_callable)
result = w.run(*args, **kwargs)   # → WorkerResult
```

| Property | Type | Meaning |
|---|---|---|
| `last` | WorkerResult\|None | Most recent result |
| `history` | list[WorkerResult] | All results ever |

---

## `WorkerResult`

| Field | Type | Meaning |
|---|---|---|
| `status` | Status | PENDING / RUNNING / DONE / FAILED |
| `value` | Any | Return value of fn |
| `error` | str\|None | Traceback if FAILED |
| `elapsed_s` | float | Wall time in seconds |
| `ok` | bool (property) | True iff status == DONE |

---

## `BatchWorker`

```python
batch = BatchWorker(workers=[w1, w2, w3])
results = batch.run_all(
    args_list=[(x0_a,), (x0_b,), (x0_c,)],
    kwargs_list=[{}, {}, {}],
)
batch.all_ok     # True iff all DONE
batch.summary()  # list of {worker, status, elapsed_s, error}
```

---

## `Status` enum

```python
class Status(Enum):
    PENDING = auto()
    RUNNING = auto()
    DONE    = auto()
    FAILED  = auto()
```

---

## Tests

```
tests/test_workers.py
```

---

## Auto-linked

→ [[attractors]]
→ [[cursor-skills]]
→ [[2026-07-16-011935-mcp-tool-command-reference]]
→ [[environment]]
→ [[COMMANDS]]
→ [[queue]]

→ [[mcp-server-tools-init]]
→ [[mcp-index]]
→ [[mcp-server-main]]
→ [[mcp-server-server]]
→ [[workers-base]]
→ [[mcp-server-tools-system]]

---

## Semantic links

→ [[workers]]
→ [[workers-base]]
→ [[workers]]
→ [[workers-cairrn-worker]]
→ [[mcp-server]]

## Related notes

→ [[cursor-ingest/2026-08-04-072347-workers-base-py]]
→ [[cursor-ingest/2026-08-04-072347-source-workers-base-md]]
→ [[cursor-ingest/2026-08-04-072347-mcp-server-md]]
→ [[cursor-ingest/2026-08-04-072347-code-md]]
→ [[cursor-ingest/2026-08-04-072347-sims-md]]

→ [[cursor-ingest]] — import index  

*Imported by `graph/ingestion.py` — links via MiniLM all-MiniLM-L6-v2*
