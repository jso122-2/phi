# phi / ui / qt / tabs.py

#source #python

> path: phi/ui/qt/tabs.py  
> ext: .py  

---

# phi / ui / qt / tabs.py

phi.ui.qt.tabs — PySide6 tabbed view container.

Replaces phi.ui.tabs.TabbedView (tk.Frame).

Exposes:
    TabbedView   — QTabWidget wrapper (owns all sub-views)
    AlbumView    — two-pane album browser (QWidget)
    ArtistView   — two-pane artist browser (QWidget)
    ModelStatusView — model registry table (QWidget)

Public API (mirrors TabbedView)
---------------------------------
    tracks_view   — PlaylistWidget (backward-compat with app.playlist_panel)
    albums_view   — AlbumView
    artists_view  — ArtistView
    models_view   — ModelStatusView
    enrich_view   — stub (QWidget)
    

Defines: AlbumView, ArtistView, ModelStatusView, _NullTabView, TabbedView, __init__, _build, on_focus, select_album, _on_album_select, _on_album_double, _on_track_double, _play_all, __init__, _build, on_focus, select_artist, _on_artist_select, _on_artist_double, _on_track_double, _play_all, __init__, _build, on_focus, _run_all, _run_model, _on_progress, _on_done, on_focus, __init__, _build, _on_tab_changed, switch_to, _worker

---

## Semantic links

→ [[2026-07-28-230105-remaining-refactor-pass-port-all-remaining]]
→ [[engine-phi-session]]

## Related notes

→ [[cursor-ingest/2026-08-04-072347-phi-ui-qt-rooms-library-room-py]]
→ [[cursor-ingest/2026-08-04-072347-phi-ui-qt-overlay-py]]
→ [[cursor-ingest/2026-08-04-072347-phi-ui-qt-init-py]]
→ [[cursor-ingest/2026-08-04-072347-phi-ui-qt-app-overlays-py]]
→ [[cursor-ingest/2026-08-04-072347-phi-ui-qt-sidebar-py]]

→ [[cursor-ingest]] — import index  

*Imported by `graph/ingestion.py` — links via MiniLM all-MiniLM-L6-v2*
