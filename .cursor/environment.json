{
  "name": "phi",
  "user": "ubuntu",
  "install": "sudo apt-get install -y python3.12-venv -q 2>/dev/null || true && cd /workspace && python3 -m venv .venv --system-site-packages && .venv/bin/pip install --upgrade pip setuptools wheel -q && .venv/bin/pip install -e '.[dev]' -q"
}
