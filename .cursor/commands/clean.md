---
description: Fix repo file tree.
---

The user invoked `/clean` workflow mode.

Read `.agent-context/clean.md` immediately and follow its behaviour contract for the rest of this session. Do not skip it.

Optional: call MCP `run_command` with command=`/clean` to confirm the contract path.

Fix repo file tree.
