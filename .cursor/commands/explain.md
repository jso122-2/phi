---
description: Plain-language explanation.
---

The user invoked `/explain` workflow mode.

Read `.agent-context/explain.md` immediately and follow its behaviour contract for the rest of this session. Do not skip it.

Optional: call MCP `run_command` with command=`/explain` to confirm the contract path.

Plain-language explanation.
