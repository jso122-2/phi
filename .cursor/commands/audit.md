---
description: Three-layer health audit.
---

The user invoked `/audit` workflow mode.

Read `.agent-context/audit.md` immediately and follow its behaviour contract for the rest of this session. Do not skip it.

Optional: call MCP `run_command` with command=`/audit` to confirm the contract path.

Three-layer health audit.
