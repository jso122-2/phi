---
description: Build mode — write, run, iterate.
---

The user invoked `/dev` workflow mode.

Read `.agent-context/dev.md` immediately and follow its behaviour contract for the rest of this session. Do not skip it.

Optional: call MCP `run_command` with command=`/dev` to confirm the contract path.

Build mode — write, run, iterate.
