---
description: Surgical inline fixes.
---

The user invoked `/edit` workflow mode.

Read `.agent-context/edit.md` immediately and follow its behaviour contract for the rest of this session. Do not skip it.

Optional: call MCP `run_command` with command=`/edit` to confirm the contract path.

Surgical inline fixes.
