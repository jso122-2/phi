---
description: Package raw output cleanly.
---

The user invoked `/modular` workflow mode.

Read `.agent-context/modular.md` immediately and follow its behaviour contract for the rest of this session. Do not skip it.

Optional: call MCP `run_command` with command=`/modular` to confirm the contract path.

Package raw output cleanly.
