---
description: Connect imports, interfaces, pipeline.
---

The user invoked `/wire` workflow mode.

Read `.agent-context/wire.md` immediately and follow its behaviour contract for the rest of this session. Do not skip it.

Optional: call MCP `run_command` with command=`/wire` to confirm the contract path.

Connect imports, interfaces, pipeline.
